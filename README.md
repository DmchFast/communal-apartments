# communal-apartments 🏢

 **Сервис учёта коммунальных показаний** — консольное приложение на Python с объектной моделью предметной области.

[![Python](https://img.shields.io/badge/3.10+-3776AB?style=for-the-badge&logo=python&logoColor=FFD43B&label=Python&labelColor=303030)](https://www.python.org/)
[![JSON](https://img.shields.io/badge/JSON-000000?style=for-the-badge&logo=json&logoColor=white&label=JSON&labelColor=303030)](https://www.json.org/)
[![Pytest](https://img.shields.io/badge/9.1.1-0A9EDC?style=for-the-badge&logo=pytest&logoColor=0A9EDC&label=Pytest&labelColor=303030)](https://pytest.org/)
[![Flake8](https://img.shields.io/badge/7.3.0-3B78B3?style=for-the-badge&logo=python&logoColor=3B78B3&label=Flake8&labelColor=303030)](https://flake8.pycqa.org/)

---

## Назначение

Приложение для учёта показаний счётчиков воды, электроэнергии, газа и других коммунальных ресурсов. Позволяет фиксировать показания, рассчитывать расход, вести историю и рассчитывать платежи по тарифам.

**Целевая аудитория:**

- Жильцы квартир и частных домов
- Семьи, планирующие бюджет на коммунальные услуги
- Управляющие компании

---

## Предметная область

Сервис предоставляет возможность добавлять счётчики, вносить показания с автоматической фиксацией даты и времени, проверять корректность (новое показание не может быть меньше предыдущего), вычислять расход как разность двух последних показаний и рассчитывать платёж по установленному тарифу. Данные каждого пользователя хранятся в отдельных JSON-файлах.

---

## Основные сущности

- **Пользователь** — владелец счётчиков (вход по имени)
- **Счётчик** — устройство учёта (название, единица измерения, тариф)
- **Показание** — значение с датой и временем снятия
- **Платёж** — сумма к оплате на основе расхода и тарифа

---

## Основные классы

### `Meter` — счётчик

Объект представляет устройство учёта коммунального ресурса.

**Атрибуты:**

- `id` — уникальный идентификатор счётчика;
- `name` — название счётчика;
- `unit` — единица измерения;
- `tariff` — тариф за единицу ресурса (`None`, если не установлен).

**Методы:**

- `__str__()` — строковое представление вида `[1] Вода (м^3), тариф: 45.50 руб.`;
- `set_tariff(tariff)` — установить или сбросить тариф;
- `from_data(meter_id, data)` — создать объект из JSON (`@classmethod`);
- `to_dict()` — преобразовать объект в данные для JSON.

---

### `Reading` — показание

Объект представляет одно показание счётчика.

**Атрибуты:**

- `meter_id` — ID счётчика, к которому относится показание;
- `value` — значение показания;
- `date` — дата снятия (проставляется автоматически, если не передана);
- `time` — время снятия (проставляется автоматически, если не передано).

**Методы:**

- `__str__()` — строковое представление вида `2026-09-15 10:00:00 - 100.0`;
- `from_data(data)` — создать объект из JSON (`@classmethod`);
- `to_dict()` — преобразовать объект в данные для JSON.

---

### `User` — пользователь

Объект представляет владельца счётчиков и показаний.

**Атрибуты:**

- `name` — имя пользователя;
- `meters: list[Meter]` — коллекция объектов `Meter`;
- `readings: list[Reading]` — коллекция объектов `Reading`.

**Методы:**

- `__str__()` — строковое представление пользователя.

---

## Взаимодействие объектов

- Объект `User` хранит коллекции `Meter` и `Reading`.
- `Reading.meter_id` ссылается на `Meter.id`.
- При загрузке JSON данные преобразуются в объекты через `from_data()`.
- При сохранении объекты преобразуются обратно в JSON через `to_dict()`.
- Функции работы с коллекциями (`add_meter`, `get_history`, `calculate_consumption` и т. д.) остаются обычными функциями модуля, но работают с объектами предметной области.

---

 ## Структура проекта

```
communal-apartments/
├── main.py              # точка входа, меню и обработчики
├── storage.py           # сохранение/загрузка JSON ↔ объекты
├── utils.py             # вспомогательные функции ввода
├── models/              # пакет классов предметной области
│   ├── __init__.py
│   ├── meters.py        # класс Meter + функции работы со счётчиками
│   ├── readings.py      # класс Reading + функции работы с показаниями
│   ├── tariffs.py       # установка тарифа и расчёт платежа
│   └── users.py         # класс User
├── data/                # JSON-файлы данных
│   ├── .gitkeep
│   ├── meters.json
│   └── readings.json
├── tests/               # автоматизированные тесты pytest
│   ├── conftest.py
│   ├── test_meters.py
│   ├── test_readings.py
│   ├── test_storage.py
│   ├── test_tariffs.py
│   └── test_utils.py
├── .flake8              # конфигурация линтера
├── pytest.ini           # конфигурация тестов
├── requirements.txt     # зависимости
└── README.md
```

---

 ## Формат хранения данных

 Данные хранятся в JSON в каталоге `data/`. Для каждого пользователя используются отдельные записи.

 ### Пример записи счётчика

 Файл `data/meters.json`:

```
{
  "Пользователь": {
    "1": {
      "name": "Вода",
      "unit": "м^3",
      "tariff": 45.5
    }
  }
}
```

 ### Пример записи показания

 Файл `data/readings.json`:

```
{
  "Пользователь": [
    {
      "meter_id": 1,
      "value": 2324.0,
      "date": "2026-09-15",
      "time": "08:32:37"
    }
  ]
}
```

 При загрузке JSON преобразуется в объекты `Meter` и `Reading`.

 При сохранении объекты преобразуются обратно в JSON.

---

## Основные возможности

### Работа со счётчиками

Пользователь может:

- просматривать список счётчиков;
- добавлять новый счётчик;
- указывать название и единицу измерения;
- устанавливать тариф;
- удалять счётчик.

### Работа с показаниями

Пользователь может:

- добавлять новые показания;
- автоматически сохранять дату и время;
- просматривать историю;
- удалять показания;
- рассчитывать расход между последними показаниями.

### Работа с тарифами

Для каждого счётчика можно установить тариф.

Стоимость рассчитывается по формуле:

```
платёж = расход × тариф
```

где:

- `расход` — разница между текущим и предыдущим показанием;
- `тариф` — стоимость одной единицы коммунального ресурса.

---

## План развития

На следующих этапах планируется:

- Расширение функциональности
- Создание веб-приложения на Django с базовой структурой
- Подключение базы данных
- Создание форм для ввода и редактирования данных
- Реализация пользователей, регистрации и аутентификации
- Разработка REST API
- Docker
- Настройка CI/CD для автоматизации развертывания

---

 ## Запуск

```
# Переход в папку проекта
cd communal-apartments

# Активация виртуального окружения
.venv\Scripts\activate

# Установка зависимостей
python -m pip install -r requirements.txt

# Запуск программы
python main.py
```

---

## Проверка проекта

Запуск тестов:

```bash
pytest -v
```

 Тесты проверяют:

 - создание объектов `Meter`, `Reading`, `User` и значения их атрибутов;
- строковое представление объектов;
- работу методов `set_tariff`, `from_data`, `to_dict`;
- операции с коллекциями объектов;
- корректность преобразования JSON ↔ объекты;
- обработку исключений;
- работу вспомогательных функций ввода.

---

Проверка стиля кода:

```bash
flake8 .
```

 Проект использует стиль оформления кода PEP 8.
